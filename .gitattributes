# В репозитории включён autocrlf, а shell-скрипты с CRLF не запускаются в Linux:
# gradlew из CI падал бы с «bad interpreter: /bin/sh^M».
gradlew text eol=lf
*.sh text eol=lf

# Бинарь wrapper'а не должен трогаться конвертацией переводов строк.
gradle-wrapper.jar binary
