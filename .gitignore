/.claude/

# Секреты — только через .env, никогда в git (CLAUDE.md, «Конвенции»)
.env
.env.*
!.env.example

# Локальные правки compose-стека: подмена портов, отладочные переменные.
docker-compose.override.yml
