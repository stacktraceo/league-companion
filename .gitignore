
# Секреты - только через .env, никогда в git (DECISIONS.md, «Конвенции»)
.env
.env.*
!.env.example

# Локальные правки compose-стека: подмена портов, отладочные переменные.
docker-compose.override.yml
