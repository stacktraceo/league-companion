# Секреты не должны попадать в контекст сборки ни при каких условиях
# (CLAUDE.md, «Конвенции»). Слой с ними остался бы в истории образа.
.env
.env.*
!.env.example

# Для сборки не нужно.
Dockerfile
.dockerignore
README.md
